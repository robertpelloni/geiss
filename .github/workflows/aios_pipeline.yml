name: Omni-Workspace CI/CD - aios
'on':
  push:
    paths:
    - aios/**
  pull_request:
    paths:
    - aios/**
jobs:
  build-test-deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      with:
        submodules: recursive
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: 3.x
    - name: Run Pipeline for aios
      run: python3 scripts/deployment_pipeline.py aios staging
